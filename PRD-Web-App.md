# وثيقة متطلبات المنتج (PRD)
## واجهة الويب — منصة إدارة الموظفين (لوحة المالك/المدير)

---

### 1. نظرة عامة

**اسم المنتج:** لوحة تحكم الويب لإدارة الموظفين (اسم مؤقت)

**الغرض:** تطبيق ويب يستخدمه المالك والمدير لمتابعة أداء الموظفين، إدارة المهام، عرض الإحصائيات، وإدارة إعدادات النظام والبوت.

**الجمهور المستهدف:** المالك والمدير فقط (استخدام داخلي).

**التقنية المقترحة:** Next.js + Tailwind CSS، متصل بقاعدة بيانات Supabase (نفس القاعدة التي يتصل بها برنامج الدسكتوب)، ومنشور على Vercel.

---

### 2. المشكلة التي يحلها التطبيق

- حاجة المالك والمدير لرؤية شاملة لأداء كل الموظفين من مكان واحد دون التقيد بجهاز معيّن
- إدارة إعدادات النظام (الموظفين، الصلاحيات، توكن بوت التلكرام) من واجهة مركزية
- متابعة الإحصائيات والتقارير دون الحاجة لبرنامج الدسكتوب

---

### 3. المستخدمون وصلاحياتهم

| الدور | الصلاحيات على الويب |
|---|---|
| **المالك** | إدارة كاملة: الإعدادات العامة، إضافة توكن بوت التلكرام، إضافة/حذف/تعديل الموظفين وبياناتهم، عرض كل الإحصائيات |
| **المدير** | إسناد المهام، إضافة/حذف موظفين وتحديد بياناتهم عند الإضافة، عرض كل الإحصائيات والتقارير |

*(الموظف العادي لا يستخدم واجهة الويب — فقط برنامج الدسكتوب)*

---

### 4. الميزات الأساسية

#### 4.1 إدارة الموظفين
- إضافة موظف جديد: الاسم، رقم الهاتف، المهنة، معرف تلكرام (Telegram ID)
- تحديد عدد ساعات العمل المطلوبة يومياً لكل موظف
- حذف أو تعديل بيانات موظف موجود
- تحديد مهمة/دور الموظف مباشرة عند إضافته (بدون تقسيم لأقسام)

#### 4.2 إسناد المهام
- إنشاء مهام جديدة وإسنادها لموظف معيّن
- عرض حالة كل مهمة (مطلوب / قيد العمل / منجز) بشكل لحظي (نفس البيانات من نظام Kanban في الدسكتوب)
- إرسال إشعار تلكرام تلقائي فور إسناد المهمة، موضحاً اسم من أسند المهمة

#### 4.3 لوحة الإحصائيات (Dashboard)
تعرض لكل موظف مقياسين منفصلين:
- **ساعات الدوام:** المدة من تسجيل الدخول إلى تسجيل الخروج (شاملة فترات الخمول)
- **وقت العمل:** الوقت الفعلي الذي قضاه الموظف بالعمل فعلياً (باستثناء الخمول)
- نسبة الدوام الشهري (بناءً على الساعات المطلوبة مقابل ساعات الدوام الفعلية)
- عدد المشاريع أو المهام المنجزة
- تفاصيل كل مهمة منجزة وتاريخها
- سجل التقارير اليومية المرسلة

#### 4.4 إعدادات بوت التلكرام
- حقل لإدخال/تحديث توكن بوت التلكرام (Bot Token)
- الإشعارات تُرسل مباشرة عبر Telegram Bot API لحظة وقوع الحدث، من نفس Route Handler ضمن Next.js الذي يعالج ذلك الحدث — دون الحاجة لعملية خلفية دائمة
- الإشعارات المرسلة تشمل:
  - إسناد مهمة جديدة (توضيح من أسندها)
  - طلب مراجعة موجّه من موظف لموظف آخر
  - أي حركة على مهمة (بحسب من يحتاج يعرف)

#### 4.5 التقارير اليومية
- عرض التقارير التي يرسلها الموظفون عند انتهاء دوامهم
- كل تقرير يظهر: اسم الموظف، ملخص المهام المنجزة/المتحركة، عدد الساعات المحتسبة، التاريخ

---

### 5. خارج النطاق (Out of Scope)

- لا يوجد نظام ملفات أو صور — التخزين نصوص فقط
- لا يوجد Timeline
- لا واجهة ويب للموظف العادي (فقط دسكتوب)
- لا تقسيم بالأقسام (Departments)

---

### 6. متطلبات التصميم (UI/UX)

- الواجهة كاملة باللغة **العربية** مع دعم RTL كامل
- أسلوب تصميم بسيط مستوحى من واجهات **أبل**
- خط مقترح: IBM Plex Sans Arabic أو Dubai Font
- دعم الوضع الليلي (Dark Mode)
- تصميم متجاوب (Responsive) ليعمل بسلاسة على شاشات مختلفة

---

### 7. البنية التقنية المقترحة

- **إطار العمل:** Next.js (يجمع الواجهة الأمامية وطبقة الـ Backend عبر API Routes / Route Handlers ضمن نفس المشروع)
- **التنسيق:** Tailwind CSS
- **قاعدة البيانات:** Supabase (مشتركة مع برنامج الدسكتوب)
- **الاستضافة:** Vercel — تكامل طبيعي مع Next.js (نفس الشركة المطورة)، والنشر يتم مباشرة من مستودع الكود (Git-based Deployment) بدون إدارة سيرفر يدوياً

**ملاحظة تقنية مهمة بخصوص بوت التلكرام على Vercel:**
بما أن Vercel لا يشغّل عمليات دائمة (لا يوجد سيرفر يبقى "مستمعاً" طوال الوقت كما في Raspberry Pi)، فإن آلية استقبال رسائل/أوامر البوت (إن وُجدت) يجب أن تعتمد على **Webhook** بدلاً من Polling: يُسجَّل رابط (Route Handler ضمن Next.js) كـ Webhook لدى تلكرام، وعند حدوث أي تفاعل، تلكرام نفسه يستدعي هذا الرابط. أما إرسال الإشعارات (الاتجاه الوحيد المطلوب حالياً: من النظام إلى الموظف) فهذا لا يحتاج عملية دائمة أصلاً — يكفي استدعاء Telegram Bot API (`sendMessage`) لحظة وقوع الحدث (إسناد مهمة، طلب مراجعة) من داخل نفس الـ Route Handler الذي يعالج ذلك الحدث، وهذا يعمل بشكل طبيعي وفوري على Vercel دون أي تعديل إضافي.

---

### 8. الاعتماديات المشتركة مع برنامج الدسكتوب

- قاعدة بيانات Supabase موحّدة بين المنصتين
- أي تغيير من الويب (مثل إسناد مهمة) ينعكس فوراً على الدسكتوب عند توفر الاتصال، والعكس صحيح
- توكن بوت التلكرام يُدار من الويب فقط، لكنه يُستخدم لإرسال إشعارات ناتجة عن أحداث تصير في كلا المنصتين، عبر Route Handlers ضمن Next.js على Vercel

---

### 9. مؤشرات النجاح (لاحقاً)

- دقة الإحصائيات المعروضة مقارنة بالبيانات الفعلية
- سرعة وصول إشعارات التلكرام بعد أي حدث (إسناد مهمة، طلب مراجعة)
- سهولة استخدام المالك/المدير للوحة دون تدريب معقد
